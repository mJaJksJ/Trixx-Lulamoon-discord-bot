/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { StudiosListSelectItem } from '../../models/studios-list-select-item';

export interface ApiStudiosIdGet$Plain$Params {
  id: number;
}

export function apiStudiosIdGet$Plain(http: HttpClient, rootUrl: string, params: ApiStudiosIdGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<StudiosListSelectItem>> {
  const rb = new RequestBuilder(rootUrl, apiStudiosIdGet$Plain.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {"style":"simple"});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StudiosListSelectItem>;
    })
  );
}

apiStudiosIdGet$Plain.PATH = '/api/Studios/{id}';
